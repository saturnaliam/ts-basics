export function add(a: number, b: number): number {
    // return a + b
    return 0;
}

export function addPositive(a: number, b: number, c: number): number {
    // add a, b, and c, but do not add negative numbers to the sum
    return 0;
}

export function isEven(a: number): boolean {
    // return if a is even or odd (true = even, odd = false)
    return false;
}

export function sumArray(a: number[]): number {
    // add all elements of an array together
    // e.g. [1, 2, 3] -> 1 + 2 + 3 -> 6
    return 0;
}

export function duplicateElements(a: number[]): number[] {
    // return a with every element duplicated
    // e.g. [1, 2, 3] -> [1, 1, 2, 2, 3, 3]
    return [];
}